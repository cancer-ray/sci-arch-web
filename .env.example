# sci-arch frontend env (CRA). Copy to .env. These are PUBLIC (browser-shipped) values.
REACT_APP_SUPABASE_URL=https://rwfeypedveoawxnjqjch.supabase.co
# Supabase Settings -> API Keys -> Publishable key (anon). Safe in the browser; RLS governs access.
REACT_APP_SUPABASE_ANON_KEY=
# The Fastify GMP backend. Dev: http://localhost:8080. Prod: https://api.sci-arch.ca (or Vercel /api rewrite).
REACT_APP_BACKEND_URL=http://localhost:8080
